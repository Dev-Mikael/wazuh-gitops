# Generated DFIR-IRIS SealedSecrets

This folder is populated by `.github/workflows/generate-wazuh-sealed-secrets.yml`.

Expected generated file:

- `dfir-iris-secrets.yaml`

Only encrypted `SealedSecret` manifests belong here.
